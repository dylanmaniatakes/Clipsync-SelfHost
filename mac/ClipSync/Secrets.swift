import Foundation

// Fallback encryption key removed — all encryption keys must be established during pairing.
// If sharedSecretHex is nil, the device needs to complete the pairing flow.
enum Secrets {}
